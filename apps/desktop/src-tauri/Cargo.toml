[package]
name = "pocket-agent-desktop"
version = "0.1.0"
description = "Pocket Agent desktop shell"
authors = ["pocket-agent"]
edition = "2021"

[lib]
name = "pocket_agent_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
