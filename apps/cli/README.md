# CLI app (`apps/cli`)

Placeholder for **pocket-agent-cli** — terminal client for Pocket Agent.

## Status

**Future** — not cloned by `pocket-agent init` yet (`enabled: false` in `config/apps.yaml`).

Repository: [pocket-agent/pocket-agent-cli](https://github.com/pocket-agent/pocket-agent-cli)

## Deploy

Desktop install only — not deployed to Cloudflare.

## Init (when enabled)

```bash
pocket-agent init --only cli
```
